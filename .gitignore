# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# Jupyter
.ipynb_checkpoints/

# Environment
.env

# Data & artefacts. Figures, model cards, metadata and the (small) fitted pipelines are committed so the README
# renders and the hosted API serves exactly the models the cards describe. Regenerate with `edupulse train --all`.
data/processed/
mlruns/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
.coverage

# Stray screenshots in the project root
/*.png
/*.jpg

# Personal study guide, not part of the repo
guide.pdf
